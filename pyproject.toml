[project]
name = "uva-course-assistant"
version = "0.1.0"
description = "AI-powered course planning assistant for UVA students"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-dir]
"" = "src"

